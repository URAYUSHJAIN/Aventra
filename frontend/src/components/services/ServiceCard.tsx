import type { Service } from '../../data/services'
export function ServiceCard({service}:{service:Service}){const Icon=service.icon;const content=<><span className="service-number">{service.number}</span><Icon className="service-icon" size={25}/><h3>{service.title}</h3><p>{service.description}</p>{service.href&&<span className="service-link">Open analysis →</span>}</>;return service.href?<a id={`service-${service.number}`} className="service-card service-card-link" href={service.href}>{content}</a>:<article id={`service-${service.number}`} className="service-card">{content}</article>}
